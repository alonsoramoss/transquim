
import HeroUpContent from "../sub/HeroUpContent"
import HeroDownContent from "../sub/HeroDownContent"

const Hero = () => {
  return (
    <section id="inicio">
      <HeroUpContent/>
      <HeroDownContent/>
    </section>
  );
};

export default Hero;
